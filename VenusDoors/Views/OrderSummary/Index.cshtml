
@{
    ViewBag.Title = "Order Summary";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@*<style>
        #tddelete{
            text-align:center;
        }

        #tddelete a i{
            color:red;
            font-size:20px;
        }

        #tddelete a i:hover{
            color:#bb0000;
        }
    </style>*@

<div class="br-pageheader pd-y-15 pd-l-20">
    <nav class="breadcrumb pd-0 mg-0 tx-12">
        <a class="breadcrumb-item" href="/">Venus Doors</a>
        <span class="breadcrumb-item active">@ViewBag.Title</span>
    </nav>
</div>

<div class="br-pagebody">
    <div class="br-section-wrapper">
        <div class="card-header pd-sm-bottom-30" style="background-color: #ffffff00;">
            <h4 class="tx-gray-800 mg-b-5">Order Summary</h4>
            @if (ViewBag.xUserDoors == null)
            {
                <h3 style="color:red">No items</h3>
            }
            else
            {
                <p class="mg-b-0">Modals are streamlined, but flexible, dialog prompts with the minimum required functionality and smart defaults.</p>
            }

        </div>

        <table class="table table-responsive mg-b-0 tx-12">
            <thead>
                <tr class="tx-10">
                    <th>Preview</th>
                    <th>Name a Door</th>
                    <th>Outside profile</th>
                    <th>Inside profile</th>
                    <th>Flat Panel</th>
                    <th>Quantity</th>
                    <th>Sub-Total</th>
                    <th>Delete item</th>
                </tr>
            </thead>
            <tbody id="id-table">
                @if (Session["UserID"] == null)
                {

                }
                else
                {
                    if (ViewBag.xUserDoors == null)
                    {

                    }
                    else
                    {
                        foreach (Model.DoorsxUser i in ViewBag.xUserDoors)
                        {
                            
                            <tr>
                                <td><img src="@i.Picture"></td>
                                <td>@i.Material.Description</td>
                                <td>@i.OutsideEdgeProfile.Description</td>
                                <td>@i.InsideEdgeProfile.Description</td>
                                <td>@i.PanelMaterial.Description</td>
                                <td>@i.Quantity</td>
                                <td>@i.SubTotal $</td>
                                <td id="tddelete">
                                    <button class="btn btn-danger btn-icon btnn-dele" data-id="@i.Id" style="width: 37px;height: 37px;" type="submit"><i class="fa fa-trash"></i></button>
                                </td>
                            </tr>
                        }
                    }
                }

            </tbody>
        </table>

        <div class="row">
            <div class="col-md-7">

            </div>
            <div class="col-md-5">
                <center>
                    <button id="btn_carrito" type="button" href="~/Views/ConfiDoor/Index.cshtml" class="btn btn-warning" title="">To return</button>
                    <button id="btn_carrito" style="margin-left: 10px;" type="button" src="#" class="btn btn-success" title="">Build a Door</button>
                    <button style="margin-left: 10px;" onclick="ConfirmOrder(Model.DoorsxUser.Order.Id)" type="submit" src="#" class="btn btn-primary" title="">Continue</button>
                </center>
            </div>
        </div>
    </div>
</div>
@section scripts{

    <script type="text/javascript">        
        var urlDeleteItem = '@Url.Action("DeleteItem", "OrderSummary")';
    </script>


    @Scripts.Render("~/Scripts/js/OrderSumm.js")
}